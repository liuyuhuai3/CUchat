FROM node:18-alpine

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 创建必要的目录
RUN mkdir -p uploads/images uploads/audio uploads/files

# 设置环境变量
ENV NODE_ENV=development
ENV PORT=3000

# 暴露端口
EXPOSE 3000

# 启动命令（开发模式）
CMD ["npm", "run", "dev"]